export const wrappingPricingConfig = {
  currency: 'EUR' as const,
  referenceRange: {
    min: 0,
    max: 0,
  },
  notes: ['Reservado para rotulacion, wrapping y servicios con cierre por presupuesto.'],
}
