import type { TemplateDownloadFormat } from '../types/printTemplates'

export const templateFormatLabels: Record<TemplateDownloadFormat, string> = {
  pdf: 'PDF',
  ai: 'AI',
  svg: 'SVG',
  cdr: 'CDR',
  png: 'PNG',
  webp: 'WebP',
}
